# Project Status

## 已完成

- 项目定义、纯前端 UI 原型、本地后台、统一日志、单视频真实解析和 B 站扫码登录已完成
- 下载任务已接入本机 `tools/BBDownNext/BBDown.exe` v2.0.0；使用 BBDownNext 原生协议，视频以 FFmpeg 合并为 MP4，音频使用 `--get a --mux None` 保留原始 M4A
- 下载与音频转写任务共用持久化本地串行队列：支持取消、移除等待项、失败重试、清理历史；下载目录实际生效并会自动创建、检查写入权限
- 下载状态使用真实阶段，不伪造百分比；取消后保留无法安全识别的临时文件
- 日志记录安全的任务阶段和输出位置；本轮日志凭据格式扫描结果为 0

## 当前

- 使用真实视频 `BV1eQNL6JEV2` 完成视频和音频下载；FFprobe 确认 MP4 含视频/音频轨、M4A 含音频轨。产物大小分别为 243,509,334 字节和 93,629,200 字节，位于 `C:\Users\Atlas\Videos\BiliScribe`
- 已实际验证严格串行、当前任务取消后下一项自动开始、等待任务移除、失败任务重试、目录持久化/不可写错误及清理历史；清理历史未删除媒体文件
- `npm run dev`、`npm run build`、正式服务页面与 API、VBS 已有实例复用检查通过；正式健康接口报告 BBDownNext v2.0.0 可用
- `node --check server/api.js`、`git diff --check` 通过；构建成功

## 问题

- 取消任务后，BBDownNext 留下的部分文件或临时文件不会自动删除，避免误删用户文件
- 下载进度无法稳定获取百分比，界面显示真实阶段（准备中/下载中/合并中）
- UP 主主页/批量下载和 MiMo 转写尚未接入；头像及封面加载问题仍保留

## 下一步

- 按后续需求接入 UP 主/批量解析或 MiMo 转写
