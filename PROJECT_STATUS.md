# Project Status

## 已完成

- 项目定义、纯前端 UI 原型、本地后台、统一日志、单视频解析、B 站扫码登录和真实视频/音频下载已完成。
- 下载使用本机 `tools/BBDownNext/BBDown.exe` v2.0.0；视频由 FFmpeg 合并为 MP4，音频保留原始 M4A。下载任务使用持久化串行队列，支持取消、移除等待项、失败重试、历史清理和打开文件位置。
- UP 主主页解析已接入：读取投稿、合集/系列及各组视频，分页结果统一转换为前端数据；未归入合集的视频归入“其他视频”。批量视频/音频下载复用现有任务队列。
- B 站头像与视频封面通过本地受限图片代理加载；代理只允许指定 B 站图片域名并校验重定向。
- Windows Explorer 打开文件时会定位并选中文件；打开目录时直接打开目标目录。

## 当前验证

- 使用 UP 主主页 UID `24715837` 实测解析：昵称“祿命闡微”、18 个投稿、14 个合集/系列、228 个可选唯一视频，另有“其他视频”分组；分页、标题搜索、展开/折叠及全部/当前页选择语义已在浏览器中验证。
- 实际浏览器确认 UP 主头像、视频封面和单视频封面经本地代理显示；图片代理返回正确图片类型，非白名单域名请求被拒绝。
- 批量提交两项视频下载和两项音频下载，均生成真实 MP4/M4A 文件并完成；任务使用原串行队列。Explorer 文件定位 API 返回成功。
- `npm run build`、`node --check server/api.js`、`git diff --check` 通过。`npm run dev` 曾在备用端口启动并提供前端与 API；正式服务健康检查通过。
- 本轮日志凭据格式扫描结果为 0；未将 Cookie 或 token 暴露给前端或日志。

## 已知问题与范围

- B 站网络、登录状态或接口策略变化可能影响主页解析；图片显示依赖 B 站图片源可访问。
- 取消下载后无法可靠识别的临时文件会保留，避免误删用户文件；BBDownNext 不稳定提供百分比时界面显示真实任务阶段。
- MiMo 转写尚未接入。

## 下一步

- 后续按需求接入 MiMo 转写；本阶段未实现 UP 主以外的批量抓取能力或高级下载设置。
