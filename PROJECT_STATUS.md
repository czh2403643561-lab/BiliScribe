# Project Status

## 已完成

- 本地服务、B 站单视频与 UP 主解析、扫码登录、BBDownNext 视频/音频下载和持久化串行队列已接入。
- UP 主合集现在采用明显的父级容器；子视频缩进显示并带层级线，搜索、分页和选择交互保留。
- 任务页已简化为“正在处理 / 已完成”两栏，显示处理/完成/失败计数；等待项可移除、运行项可取消、失败项可重试，完成项可打开位置。无真实百分比时使用阶段文字和不定进度条。
- 新下载使用分组目录：UP 主/合集或系列/视频或音频；未归入合集的视频使用“其他视频”目录。单视频使用“视频标题/视频或音频”。目录名与文件名会清理 Windows 非法字符并限制长度，合集目录同时建立“文字稿”预留文件夹。
- 文件位置通过本机 Windows Shell API 打开父目录并选中文件；目录目标直接打开。日志记录解析后的目标、目标类型及 Explorer 操作结果。

## 当前验证

- `npm run build`、`node --check server/api.js`、`git diff --check` 通过。
- 使用真实 UP 主视频实测：合集音频保存至 `祿命闡微/其他视频/音频/标题.m4a`，合集视频保存至 `祿命闡微/其他视频/视频/标题.mp4`；文字稿预留目录已创建。
- 单视频实测保存至 `单视频目录验证 中文 标题/视频/单视频目录验证 中文 标题.mp4`。含中文和空格的真实路径通过 Windows 11 Explorer 打开，Shell 检查确认对应文件已选中，而非跳至 Documents。
- 前端实测新建任务可把 UP 主与分组上下文提交给现有队列；旧任务仍在完成列表显示，原有输出路径未迁移。
- 实际运行 `BiliScribe.vbs` 后，正式服务报告 production、健康接口正常、页面返回 200 并自动打开浏览器。

## 问题与范围

- 取消下载后无法可靠识别归属的临时文件会保留；BBDownNext 不稳定提供百分比时显示真实阶段和不定进度条。
- B 站网络或登录状态可能影响解析；MiMo 转写尚未接入。

## 下一步

- 后续按需求接入 MiMo 转写；暂不迁移旧任务文件。
